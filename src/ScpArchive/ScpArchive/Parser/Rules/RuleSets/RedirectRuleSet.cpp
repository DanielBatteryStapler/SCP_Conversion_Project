#include "RedirectRuleSet.hpp"

namespace Parser{
    void handleRedirect(TreeContext& context, const Token& token){
		///TODO: make this do something
		//for now it's just going to be left as a "null" rule and just throw them away, they don't really *need* to be there, right?
    }
}
